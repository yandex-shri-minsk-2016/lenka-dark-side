include ../company/company
include ../company-info/company-info

mixin offers(service, mod, index)
    .offers(class= mod)
        +company(service.logo)
        .offers__info
            .offers__title= service.title
            +company-info(service, index)
            .offers__down-text
                .offers__text= service.phone
            if block
                block

