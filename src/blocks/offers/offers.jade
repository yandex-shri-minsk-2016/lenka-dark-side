include ../company/company
include ../company-info/company-info

mixin offers(service, mod, ind)
    .offers(class= mod)
        +company(service)
        .offers__info
            .offers__title= service.title
            +company-info(service, ind)
            .offers__down-text
                .offers__text= service.phone
            if block
                block

